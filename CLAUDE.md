# sample that illustrates how LLMs work

There's some code in showit.py that calls an LLM, with OTel instrumentation so we can count tokens.

## illustrations

animation/index.html contains a page that will be used in a video, animating some words coming in and getting broken up into tokens (eventually)
